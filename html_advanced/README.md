# HTML Advanced - Techium

This project is part of the Holberton School front-end curriculum.
The goal is to build the HTML structure of a website called **Techium** by progressively adding semantic markup and essential metadata.

## Learning Objectives

By the end of this project, you should be able to:

- Use correct HTML document structure (`<!DOCTYPE html>`, `html`, `head`, `body`)
- Set language and text direction attributes on the root element
- Add useful metadata (`charset`, `viewport`, `description`)
- Add a proper page title
- Configure favicon links
- Write semantic, accessible HTML

## Requirements

- Allowed editors: `vi`, `vim`, `emacs`
- All files must end with a new line
- Your code should be W3C compliant
- Do not use external libraries for this part

## Project Structure

This folder contains multiple incremental task files:

- `0-index.html`, `1-index.html`, `2-index.html`, ...
- `about.html`, `article.html`, `contact.html`, `latest_news.html`
- `styleguide.html` and related styleguide versions

Each numbered file represents one step of progress.

## Core HTML Boilerplate (Expected)

At minimum, your base page should include:

```html
<!DOCTYPE html>
<html lang="en" dir="ltr">
	<head>
		<meta charset="utf-8" />
		<meta
			name="viewport"
			content="width=device-width, initial-scale=1.0, viewport-fit=cover"
		/>
		<title>Homepage - Techium</title>
		<meta name="description" content="Techium is a digital agency" />
		<link rel="icon" type="image/x-icon" href="./favicon.ico" />
		<link rel="icon" type="image/png" href="./favicon.png" />
	</head>
	<body></body>
</html>
```

## How to Run

1. Open any HTML file in your browser.
2. Or use VS Code Live Server if installed.

Example:

```bash
xdg-open 0-index.html
```

## Validation

You can validate your HTML using the W3C Validator:

- https://validator.w3.org/

## Author

- Holberton School Student
